# Captura 5 - docker ps (contenedores activos)

Nota: En esta entrega se usa el servidor de desarrollo nativo de PHP (`php artisan serve`)
en lugar de Laravel Sail, tal como se configuró desde la Clase 2.

```
$ docker ps
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
(No se usa Sail en este proyecto, se usa php artisan serve directamente)
```

Servidor corriendo en: http://127.0.0.1:8000
Comando usado: php artisan serve
